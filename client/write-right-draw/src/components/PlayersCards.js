import React from "react";

const PlayersCards = ({ player1Hand }) => {
  // console.log("hmm", player1Hand.cards["0"]);
  // let newArr = Object.values(player1Hand);

  let handArr = player1Hand.cards;
  console.log(player1Hand);
  // handArr.map((card) => console.log("maybe", card));

  return (
    <>
      {/* {handArr.map((card) => { */}
      <div>suit</div>
      {/* })} */}
    </>
  );
};

export default PlayersCards;
